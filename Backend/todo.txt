Fix none existing id's for relations

data, index, length
=> global vars for all strings besides for column1, column2
=> index1,index2 for sort

WHAT ABOUT IS-A?? Add INSERT INTO (block_type) (0/1) and foreign keys??

JOINs

native user str SQL support

fill/add new datas?? maybe with some rand() for introducing a random new data set with valid data

saving tmp to .db even if columns are swapped